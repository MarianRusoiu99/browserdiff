<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: #f5f5f5;
            color: #333;
            padding: 20px;
        }
        
        .container {
            max-width: 1400px;
            margin: 0 auto;
            background: white;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
            overflow: hidden;
        }
        
        header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 30px;
        }
        
        h1 {
            font-size: 28px;
            margin-bottom: 10px;
        }
        
        .meta {
            opacity: 0.9;
            font-size: 14px;
        }
        
        .status {
            display: inline-block;
            padding: 4px 12px;
            border-radius: 4px;
            font-weight: 600;
            font-size: 12px;
            margin-left: 10px;
        }
        
        .status.identical {
            background: #10b981;
            color: white;
        }
        
        .status.within-threshold {
            background: #f59e0b;
            color: white;
        }
        
        .status.different {
            background: #ef4444;
            color: white;
        }
        
        .content {
            padding: 30px;
        }
        
        .section {
            margin-bottom: 40px;
        }
        
        .section h2 {
            font-size: 20px;
            margin-bottom: 20px;
            color: #667eea;
            border-bottom: 2px solid #f0f0f0;
            padding-bottom: 10px;
        }
        
        .comparison-grid {
            display: grid;
            gap: 20px;
        }
        
        .comparison-item {
            border: 1px solid #e5e5e5;
            border-radius: 6px;
            padding: 20px;
            background: #fafafa;
        }
        
        .comparison-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
        }
        
        .browser-name {
            font-size: 18px;
            font-weight: 600;
        }
        
        .metrics {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
            margin-top: 15px;
        }
        
        .metric {
            background: white;
            padding: 12px;
            border-radius: 4px;
            border-left: 3px solid #667eea;
        }
        
        .metric-label {
            font-size: 12px;
            color: #666;
            text-transform: uppercase;
            margin-bottom: 4px;
        }
        
        .metric-value {
            font-size: 18px;
            font-weight: 600;
            color: #333;
        }
        
        .screenshots {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 10px;
            margin-top: 15px;
        }
        
        .screenshot-box {
            border: 1px solid #ddd;
            border-radius: 4px;
            overflow: hidden;
        }
        
        .screenshot-label {
            background: #f5f5f5;
            padding: 8px;
            font-size: 12px;
            font-weight: 600;
            text-align: center;
        }
        
        .screenshot-img {
            width: 100%;
            display: block;
        }
        
        .summary {
            background: #f9fafb;
            border: 1px solid #e5e7eb;
            border-radius: 6px;
            padding: 20px;
        }
        
        .summary-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 15px;
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>
                BrowserDiff Report
                <span class="status <%= report.overallStatus %>">
                    <%= report.overallStatus.toUpperCase().replace('-', ' ') %>
                </span>
            </h1>
            <div class="meta">
                <div><strong>URL:</strong> <%= session.targetUrl %></div>
                <div><strong>Session:</strong> <%= session.sessionId %></div>
                <div><strong>Timestamp:</strong> <%= timestamp %></div>
                <div><strong>Viewport:</strong> <%= session.viewport.width %>x<%= session.viewport.height %></div>
            </div>
        </header>
        
        <div class="content">
            <div class="section">
                <h2>Summary</h2>
                <div class="summary">
                    <div class="summary-grid">
                        <div class="metric">
                            <div class="metric-label">Baseline Browser</div>
                            <div class="metric-value"><%= report.baselineBrowser %></div>
                        </div>
                        <div class="metric">
                            <div class="metric-label">Total Comparisons</div>
                            <div class="metric-value"><%= report.getAllComparisons().length %></div>
                        </div>
                        <div class="metric">
                            <div class="metric-label">Overall Status</div>
                            <div class="metric-value"><%= report.overallStatus %></div>
                        </div>
                        <div class="metric">
                            <div class="metric-label">Session Status</div>
                            <div class="metric-value"><%= session.status %></div>
                        </div>
                    </div>
                </div>
            </div>
            
            <% if (report.getAllComparisons().length > 0) { %>
            <div class="section">
                <h2>Browser Comparisons</h2>
                <div class="comparison-grid">
                    <% report.getAllComparisons().forEach(function(comparison) { %>
                    <div class="comparison-item">
                        <div class="comparison-header">
                            <div class="browser-name"><%= comparison.browserName %></div>
                            <span class="status <%= comparison.status %>">
                                <%= comparison.status.toUpperCase().replace('-', ' ') %>
                            </span>
                        </div>
                        
                        <div class="metrics">
                            <div class="metric">
                                <div class="metric-label">Diff Percentage</div>
                                <div class="metric-value"><%= comparison.metrics.diffPercentage.toFixed(4) %>%</div>
                            </div>
                            <div class="metric">
                                <div class="metric-label">Match Percentage</div>
                                <div class="metric-value"><%= comparison.metrics.matchPercentage.toFixed(4) %>%</div>
                            </div>
                            <div class="metric">
                                <div class="metric-label">Diff Pixels</div>
                                <div class="metric-value"><%= comparison.metrics.diffPixels.toLocaleString() %></div>
                            </div>
                            <div class="metric">
                                <div class="metric-label">Threshold</div>
                                <div class="metric-value"><%= (comparison.threshold * 100).toFixed(2) %>%</div>
                            </div>
                        </div>
                    </div>
                    <% }); %>
                </div>
            </div>
            <% } %>
            
            <div class="section">
                <h2>Browser Results</h2>
                <div class="comparison-grid">
                    <% session.results.forEach(function(result) { %>
                    <div class="comparison-item">
                        <div class="comparison-header">
                            <div class="browser-name"><%= result.browserName %></div>
                            <span class="status <%= result.status === 'success' ? 'identical' : 'different' %>">
                                <%= result.status.toUpperCase() %>
                            </span>
                        </div>
                        
                        <div class="metrics">
                            <div class="metric">
                                <div class="metric-label">Version</div>
                                <div class="metric-value"><%= result.browserVersion %></div>
                            </div>
                            <div class="metric">
                                <div class="metric-label">Page Load Time</div>
                                <div class="metric-value"><%= result.pageLoadTime %>ms</div>
                            </div>
                            <% if (result.errorMessage) { %>
                            <div class="metric">
                                <div class="metric-label">Error</div>
                                <div class="metric-value" style="color: #ef4444; font-size: 14px;"><%= result.errorMessage %></div>
                            </div>
                            <% } %>
                        </div>
                    </div>
                    <% }); %>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
